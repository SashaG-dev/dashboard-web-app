import LoginLayout from '../features/authentication/LoginLayout/LoginLayout';

const Login = () => {
  return (
    <main className="login-main">
      <LoginLayout />
    </main>
  );
};
export default Login;
